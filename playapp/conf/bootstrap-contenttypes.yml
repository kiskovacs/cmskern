# Bootstrap data described using the YAML notation

# Unfortunately assigning JSON content does not work, therefore reference external definitions
# siehe http://stackoverflow.com/questions/9026017/is-it-possible-to-assign-a-json-value-inside-a-yaml-definition

ContentType(article):
    group: editorial
    sortkey: 1
    slug: article
    displayName: Article
    description: the central content type for an text article.
    jsonFormFromFile: conf/content-types/article-schema.json

ContentType(articleCollection):
    group: editorial
    sortkey: 2
    slug: articleCollection
    displayName: Article Collection
    description: holding links to one or more articles.
    jsonFormFromFile: conf/content-types/articleCollection-schema.json

ContentType(image):
    group: editorial
    sortkey: 3
    slug: image
    displayName: Image
    description: the asset as also some metadata for an image.
    jsonFormFromFile: conf/content-types/image-schema.json

ContentType(imageGallery):
    group: editorial
    sortkey: 4
    slug: imageGallery
    displayName: Image Gallery
    description: referencing multiple images at a time.
    jsonFormFromFile: conf/content-types/imageGallery-schema.json

ContentType(sidebar):
    group: site
    sortkey: 1
    slug: sidebar
    displayName: Sidebar
    description: an area displaying or teaser other side content.
    jsonFormFromFile: conf/content-types/sidebar-schema.json

ContentType(node):
    group: site
    sortkey: 2
    slug: node
    displayName: Navigation Node
    description: containing links to articles and sidebars.
    jsonFormFromFile: conf/content-types/node-schema.json
